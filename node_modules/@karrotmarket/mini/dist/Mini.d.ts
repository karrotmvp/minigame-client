import { Bridge } from '@daangn/webview-bridge-modern';
import { Environment } from '@daangn/webview-bridge-modern/lib/core/getEnvironment';
import type MiniPluginPayment from './plugins/Payment';
export declare class Mini {
    bridge: Bridge;
    environment: Environment;
    payment: MiniPluginPayment;
    constructor();
    addPlugin(Plugin: new (bridge: Bridge) => MiniPluginPayment): this;
    startPreset({ preset, params, onSuccess, onFailure, onClose, }: {
        preset: string;
        params?: {
            [key: string]: string;
        };
        onSuccess: (result: any) => void;
        onFailure?: () => void;
        onClose?: () => void;
    }): void;
    close(): void;
}
